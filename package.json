{
  "name": "root",
  "private": true,
  "engines": {
    "node": "18.x"
  },
  "workspaces": [
    "packages/client",
    "packages/service"
  ],
  "scripts": {
    "start": "node packages/service/build/main.js",
    "start:client": "yarn workspace client dev",
    "build": "yarn run clean && yarn workspace service build && yarn workspace client build && yarn run build:static",
    "build:static": "mkdir -p packages/service/build/public && cp -r packages/**/dist/* packages/service/build/public/",
    "test": "yarn workspace service test && yarn workspace client test",
    "clean": "yarn workspace service clean && yarn workspace client clean",
    "clean:deps": "rm -rf ./node_modules ./packages/**/node_modules",
    "fm": "./scripts/fm.sh"
  }
}
