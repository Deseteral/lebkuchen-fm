{
  "name": "root",
  "private": true,
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "start": "node packages/service/build/main.js",
    "build": "npm run clean && lerna run build && npm run build:static",
    "build:static": "mkdir -p packages/service/build/public && cp packages/**/dist/* packages/service/build/public/",
    "test": "lerna run test",
    "clean": "lerna run clean",
    "clean:deps": "rm -rf ./node_modules ./packages/**/node_modules",
    "bootstrap": "lerna bootstrap",
    "postinstall": "npm run bootstrap",
    "fm": "./scripts/fm.sh"
  },
  "devDependencies": {
    "lerna": "4.0.0"
  }
}
